
class MovingObject {
  constructor(ctx){
    this.positions = [];
    this.ctx
    this.castle = new Image();
    this.castle.src = "./assets/"
  }

  draw(){
    // let randPos = 
    ctx.drawImage()
  }
}

// items[Math.floor(Math.random() * items.length)];